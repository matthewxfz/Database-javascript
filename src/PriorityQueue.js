var Heap = require('heap');
function PriorityQueue(max){
    this.max = max;
    this.heap = new Heap();
}



PriorityQueue.push = function(){
    if(this.heap.size() >= this.max){
       var errr = new Error('No more room is enought for the push ')
    }
}

PriorityQueue

PriorityQueue

PriorityQueue
PriorityQueue

